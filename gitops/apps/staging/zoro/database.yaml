apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: zoro-db
spec:
  instances: 3

  bootstrap:
    initdb:
      database: app
      owner: app
      secret:
        name: zoro-db-credentials

  # Point-in-Time Recovery (PITR) - uncomment to restore from backup
  # bootstrap:
  #   recovery:
  #     database: app
  #     owner: app
  #     source: source
  #     secret:
  #       name: zoro-db-credentials
  #
  # externalClusters:
  #   - name: source
  #     plugin:
  #       name: barman-cloud.cloudnative-pg.io
  #       parameters:
  #         barmanObjectName: zoro-objectstore
  #         serverName: zoro-db

  storage:
    size: 1Gi

  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: zoro-objectstore
